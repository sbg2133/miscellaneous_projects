#include<stdio.h>

// pix_idx()
// get_vector()
// sl()
// partial_integral()
// kernel
// lic
// image

void pixIdx(vector_field_t *m_field, int* x_idx, int* y_idx)
{
    // Check that these are not pointing to NULL
    assert(x_idx);
    assert(y_idx);
    for (int i = 0; i < XMAX 
    *x_idx = 1;
    *y_idx = 2;
}

int main (int argc, char *argv[])
{
    return 0;
}
